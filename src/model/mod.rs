use crate::bot::Bot;

#[allow(unused)]
pub struct ModelController {
    //TODO: add database to this
}


impl ModelController {
    #[allow(unused)]
    pub fn new(_bot: Bot) -> Self {
        Self {
            
        }
    }
}